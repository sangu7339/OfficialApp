package com.Vebturebiz.App.entity;

/** Roles used for authorization checks; future roles are prelisted as comments. */
public enum Role {
  HR, EMPLOYEE
  // MANAGER, ADMIN, TEAMLEAD, INTERN, SUPERVISOR, HRASSISTANT, RECRUITER, FINANCE, CTO, CEO
}
